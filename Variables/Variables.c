#include <stdio.h>

int main(void)
{
    int quantity = 5; // Integer variable: stores whole numbers (no decimals)
    float price = 5.99; // Float variable: stores decimal numbers (lower precision)
    double length = 5.43342; // Double variable: stores decimal numbers (higher precision)
    char Letter = 'A'; // Char variable: stores a single character

    printf("Quantity: %d");
    printf("Price: %f");
    printf("Length in CM: %lf");
    printf("Letter: %c");

    return 0;
}

